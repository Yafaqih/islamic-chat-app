# Intégration de la Boussole Qibla

## Étape 1 : Créer le dossier components

```bash
mkdir components
cp components-QiblaCompass.js components/QiblaCompass.js
```

## Étape 2 : Importer dans pages/index.js

Ajoutez cet import en haut du fichier :

```javascript
import QiblaCompass from '../components/QiblaCompass';
```

## Étape 3 : Ajouter dans le JSX

Dans la section du return, après les messages et avant la fermeture du div principal, ajoutez :

```javascript
{/* ✨ Boussole Qibla */}
{isAuthenticated && <QiblaCompass />}
```

Exemple de placement :

```javascript
return (
  <div className="min-h-screen bg-gradient-to-br from-emerald-50 via-teal-50 to-cyan-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900 transition-colors duration-200" dir="rtl">
    
    {/* Modals et sidebar existants */}
    {showPremiumModal && (...)}
    {showHistory && (...)}
    {showFavorites && (...)}

    {/* Contenu principal */}
    <div className="flex flex-col h-screen max-w-6xl mx-auto">
      {/* Header */}
      <div className="bg-gradient-to-r from-emerald-500 to-teal-600...">
        ...
      </div>

      {/* Messages */}
      <div className="flex-1 overflow-y-auto p-4 space-y-4">
        ...
      </div>

      {/* Input */}
      <div className="border-t border-gray-200 dark:border-gray-700 bg-white dark:bg-gray-800 p-4">
        ...
      </div>
    </div>

    {/* ✨ AJOUTER ICI : Boussole Qibla */}
    {isAuthenticated && <QiblaCompass />}
  </div>
);
```

## Position du bouton flottant

Le bouton de la boussole apparaîtra en bas à gauche :
```
┌─────────────────────────────────┐
│                                 │
│        Contenu du chat          │
│                                 │
│                                 │
│                                 │
│                                 │
└─────────────────────────────────┘
 🧭 ← Bouton boussole (bottom-20 left-4)
```

## Styles appliqués

- Position fixe en bas à gauche
- Bouton rond vert avec icône Navigation
- Modal plein écran au clic
- Responsive et compatible mobile
- Support mode sombre

## Permissions requises

La boussole demandera automatiquement :
1. ✅ Permission de géolocalisation
2. ✅ Permission d'orientation (iOS 13+)
3. ✅ Permission de vibration (si disponible)